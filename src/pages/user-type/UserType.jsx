import { Typography, Box } from "@mui/material";
const UserType = () => (
  <Box p={2}>
    <Typography variant="h4">User Type Page - Protected Content</Typography>
  </Box>
);

export default UserType;
